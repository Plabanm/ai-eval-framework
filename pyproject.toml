[project]
name = "ai-eval-framework"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "jiwer",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["packages*"]